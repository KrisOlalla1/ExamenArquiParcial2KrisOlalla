spring:
  application:
    name: branches_api
  data:
    mongodb:
      uri: mongodb://localhost:27017/banquito
      database: banquito

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
